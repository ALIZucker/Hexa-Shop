import { LineEffectDirective } from './line-effect.directive';

describe('LineEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new LineEffectDirective();
    expect(directive).toBeTruthy();
  });
});
