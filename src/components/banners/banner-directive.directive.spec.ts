import { BannerDirectiveDirective } from './banner-directive.directive';

describe('BannerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
